[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "budget-router"
version = "0.1.0"
description = "Train and serve calibrated, provider-neutral model routers"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.12"
authors = [{ name = "Pranav Dulepet" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
  "typer>=0.12,<1",
]

[project.urls]
Homepage = "https://github.com/pranavdulepet/budget-router"
Repository = "https://github.com/pranavdulepet/budget-router"
Documentation = "https://github.com/pranavdulepet/budget-router/blob/main/docs/open_source_router.md"
Issues = "https://github.com/pranavdulepet/budget-router/issues"
Changelog = "https://github.com/pranavdulepet/budget-router/releases"

[project.optional-dependencies]
data = ["duckdb>=1.1", "pyarrow>=17"]
ml = ["scikit-learn>=1.9,<2"]
proxy = ["fastapi>=0.116,<1", "httpx>=0.28,<1", "uvicorn>=0.35,<1"]
tinker = [
  "tinker==0.23.1",
  "tinker-cookbook==0.5.2",
  "tml-renderers==0.1.0",
]
runner = [
  "litellm==1.81.3",
  "mini-swe-agent==2.3.0",
]
test = [
  "build>=1.2",
  "coverage>=7",
  "fastapi>=0.116,<1",
  "httpx>=0.28,<1",
  "mini-swe-agent==2.3.0",
  "pytest>=8",
  "pytest-asyncio>=0.24",
  "python-dotenv>=1.0,<2",
  "pyyaml>=6",
  "ruff>=0.12,<1",
  "scikit-learn>=1.9,<2",
]
all = [
  "duckdb>=1.1",
  "litellm==1.81.3",
  "mini-swe-agent==2.3.0",
  "pyarrow>=17",
  "scikit-learn>=1.9,<2",
  "fastapi>=0.116,<1",
  "httpx>=0.28,<1",
  "uvicorn>=0.35,<1",
  "tinker==0.23.1",
  "tinker-cookbook==0.5.2",
  "tml-renderers==0.1.0",
]

[project.scripts]
budget-router = "budget_router.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/budget_router"]

[tool.hatch.build.targets.sdist]
include = [
  "/.github",
  "/artifacts",
  "/configs",
  "/docs",
  "/examples",
  "/scripts",
  "/src",
  "/tests",
  "/AGENTS.md",
  "/CITATION.cff",
  "/CONTRIBUTING.md",
  "/LICENSE",
  "/README.md",
  "/SECURITY.md",
  "/pyproject.toml",
]
exclude = [
  "/.agents",
  "/.hf-cache",
  "/.venv",
  "/dist",
  "/logs",
  "/outputs",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
asyncio_mode = "auto"
markers = [
  "local_research: requires ignored benchmark data, run outputs, or optional runner integrations",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.lint.per-file-ignores]
"scripts/tinker_protocol_smoke.py" = ["F401"]
